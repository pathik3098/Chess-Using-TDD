stages: 
  - build
  - test
  
build: 
  stage: build
  only:
    - master
  tags: 
    - ugrad
  script: 
    - mvn compile

test: 
  stage: test
  tags: 
    - ugrad
  script: 
    - mvn clean test
  artifacts:
    when: always
    reports:
      junit:
        - ./Chess/target/surefire-reports/TEST-*.xml
